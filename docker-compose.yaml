services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: pybus-db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
